# Python Type Conversion

## Explanation

Type conversion is the process of changing a value from one data type to another.

Python provides built-in functions such as:

* `int()` – converts a value to an integer
* `float()` – converts a value to a floating-point number
* `str()` – converts a value to a string
* `bool()` – converts a value to a Boolean value

## Problem Statement

Write a Python program that accepts values from the user and demonstrates different types of type conversion.

## Features

* Converts string input to integer
* Converts string input to float
* Converts numbers to strings
* Converts values to Boolean values
* Displays the converted values and their data types

## How It Works

The program takes input as strings and converts the values using Python's built-in conversion functions.

## Technologies Used

* Python 3

## Program Flow

1. Start the program.
2. Read values from the user.
3. Convert the values into different data types.
4. Display the converted values.
5. Display their data types.
6. End the program.

## Sample Input

```text id="d8m4rq"
Enter an integer value: 25
Enter a decimal value: 12.5
```

## Sample Output

```text id="k1z7wp"
Integer: 25
Type: <class 'int'>

Float: 12.5
Type: <class 'float'>

String: 25
Type: <class 'str'>

Boolean: True
Type: <class 'bool'>
```

## Key Learning

* Understanding type conversion
* Using `int()`
* Using `float()`
* Using `str()`
* Using `bool()`
* Checking data types using `type()`

## File Location

```text id="r2x9mc"
Python-Type-Conversion/type_conversion.py
```

## Repository Structure

```text id="s5v8la"
Python-Type-Conversion/
│
├── type_conversion.py
└── README.md
```

## Author

V.Harini
